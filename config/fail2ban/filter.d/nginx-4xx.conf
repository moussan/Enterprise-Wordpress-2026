# Enterprise WordPress 2026 — Fail2ban Filter: Nginx 4xx Errors
# Matches excessive 403/404 responses indicating scanning/probing.

[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD) .* (403|404|405|444)
ignoreregex = \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)
