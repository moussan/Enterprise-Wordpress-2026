# Enterprise WordPress 2026 — Fail2ban Filter: WordPress XMLRPC
# Matches any POST request to xmlrpc.php (almost always abusive).

[Definition]
failregex = ^<HOST> .* "POST /xmlrpc\.php
ignoreregex =
