# Enterprise WordPress 2026 — Fail2ban Filter: WordPress Login
# Matches failed login attempts (POST to wp-login.php returning non-302).
# A successful login returns 302 (redirect), failed returns 200 (login page again).

[Definition]
failregex = ^<HOST> .* "POST /wp-login\.php .* (200|401|403)
ignoreregex =
